# Roadmap

Open enhancements, bug fixes, and tech debt for Agent-chat in a single priority-ordered table. Completed work is recorded in the **Done** table at the bottom.

When closing an item, move its row from **Open** to **Done** and fill in the `Closed` date.

---

## Open

| Priority | Type | Item | Detail |
|:---|:---|:---|:---|
| Medium | Security | Harden hosted mutations beyond read-only (real auth + CSRF) | **(2026-06-30 analysis)** The **read-only public mode shipped 2026-06-30** (see Done) — browser mutations now 403 on the Fly mirror via `ReadOnlyMiddleware` + `AGENT_CHAT_PUBLIC_READONLY`, and `BasicAuthMiddleware` is re-wired (attaches when `AGENT_CHAT_BASIC_AUTH_PASSWORD` is set). **Still open** for when the deploy needs to *accept authenticated writes* (not just block all of them): front the deploy with real auth (Cloudflare Access / Fly proxy auth / Tailscale) and add CSRF or an auth-header requirement on mutation routes so an authenticated admin can stop/delete/manage personas on the hosted side without re-opening the public write hole. Lower priority now that the data-loss exposure is closed by read-only mode. |
| High | Testing | pytest smoke suite (isolated temp `db/chat.db`) | **(2026-06-30 analysis)** No real test suite exists — validation is manual import/compile/JSON checks. Risky now that the web UI carries auth, bidirectional sync, exports, zip parsing, persona CRUD, SSE, and orchestration. Start small with an isolated-temp-DB fixture (do **not** mock SQLite — WAL multi-process behaviour is the thing under test): `tests/test_mcp_turns.py` (turn rotation, out-of-turn rejection, max-turn completion, `done`/`blocked`/`continuous`, no-conversation), `tests/test_web_routes.py` (Starlette `TestClient`), `tests/test_sync.py` (local/remote conflict + delete propagation), `tests/test_personas.py` (parser/import incl. malformed frontmatter + zip limits), `tests/test_inspect_tail.py` (regression for the premature-complete bug below). Wire into the existing "Lightweight CI workflow" row (CI runs `pytest` + `compileall` + JSON/TOML validation). |
| Medium | Tech debt | Split `src/web_ui.py` (~3,890 lines) into modules | **(2026-06-30 analysis)** Single file now holds schema, DB access, Markdown render, sync API, homepage/conversation/persona/orchestrate templates+CSS+JS, auth middleware, and the route table — the main maintainability drag. Mechanical, low-risk refactor to do **before** the next big UI feature: `web/db.py` (connection/schema/conversation+persona helpers), `web/render/` (per-page rendering), `web/api/` (sync/conversations/personas/orchestrate handlers), `web/security.py` (basic+bearer auth, CSRF), `web/assets.py` (CSS/JS constants); keep `web_ui.py` as the app-assembly + route-table entrypoint and **keep route paths unchanged**. Smoke-test after each move. Soft-depends on the pytest row (tests make the refactor safe). |
| Medium | Tech debt | Centralize `SCHEMA` + migrations into one module | **(2026-06-30 analysis)** `SCHEMA`/`_MIGRATIONS` are duplicated across `src/agent_chat_mcp.py`, `src/web_ui.py`, and `src/orchestrator/seeding.py` — CLAUDE.md already warns they must stay in sync, a predictable footgun for every future column add (share tokens, tags, stats, persona versions, explicit conversation ids). Move to `src/db/schema.py` exporting `init_db(conn)` + `apply_migrations(conn)`; have all three entrypoints import it. Add a schema test that boots each entrypoint against an empty temp DB. Pairs naturally with the `web_ui.py` split. |
| Medium | Enhancement | Refresh stale homepage / marketing copy (six CLIs, Gemini fallback, personas first-class) | **(2026-06-30 analysis)** The live homepage still says "Three CLIs" / "Claude Code, Codex, Gemini" while the repo supports six CLIs with Gemini as a deprecated fallback. Stale spots in `src/web_ui.py`: hero copy (~L1450), "Three CLIs" heading (~L1490), registration snippets (~L1623), sample participants `claude-code,gemini` (~L1646), and the "CLIs" resource card (only lists Claude Code/Codex/Antigravity). Update: "Six CLIs"; lead with Claude Code, Codex, Antigravity, Kimi, OpenCode (Gemini marked fallback); switch examples to `claude-code,antigravity` (or `…,codex,antigravity`); add Kimi/OpenCode to the CLI card. While in there, **sell the actual product**: a persona roster / role-lab section, a "Launch a debate" CTA that explains local-vs-hosted, a supported-CLI matrix, "latest debates" showing persona names (not just participants), and a one-line public-read-only explanation. Also sweep OG metadata + any four-CLI preflight mentions in `docs/App/web-ui.md`. |
| Medium | Enhancement | Guard hosted `/orchestrate` (local-only; disable POST on Fly) | **(2026-06-30 analysis)** The hosted `/orchestrate` route is public and interactive but can't see local CLI configs or spawn local agents, so it's a dead end for anyone but the local operator. Detect hosted mode via env var and either hide the nav link or render it as a "local only" panel with the exact local command to run; reject `POST /api/orchestrate` with a clear message when hosted/read-only. Keep local `/orchestrate` fully functional. Overlaps the "Orchestrator: sidecar awareness" row (both touch the `/orchestrate` page) — coordinate the two when either ships. |
| Medium | Tech debt | Dependency + deploy hygiene (Win/Linux locks, Dependabot, `/healthz`, deploy smoke) | **(2026-06-30 analysis)** Deps are pinned (good) but the `pip freeze` set is Windows-generated and the Dockerfile strips `pywin32` for the Linux deploy — a smell. Generate separate Windows-local vs Linux-deploy lock outputs (or a constraints file excluding Windows-only packages on Linux); add Dependabot or a scheduled dependency check. For deploy confidence: add a `/healthz` route that checks DB readability/writability + schema version, and a cheap post-`fly deploy` smoke test that hits it. |
| Medium | Enhancement | Moderator / Judge / Summarizer / Fact-checker roles (end-of-run scoring + summary) | **(2026-06-30 analysis)** The most valuable feature expansion: a third role type beyond debaters. **Moderator** keeps turns on track and asks follow-ups (the Phase 2b `Debate-Hosts/` → `continuous`-mode host is the seed of this); **Judge** scores claims/consistency/evidence/persuasion against a rubric; **Summarizer** emits an executive summary + unresolved questions at completion (persist it so transcripts stay useful after the run, and surface on `/conversations/<id>` + in the zip bundle); **Fact-checker** flags unsupported claims. Grow the existing `preset` concept to carry role assignments. Builds on the persona registry + Phase 2b spawn machinery. |
| Medium | Enhancement | Additional conversation modes / preset templates | **(2026-06-30 analysis)** The MCP loop supports more than debates — grow `preset` into a richer mode catalog beyond the current `debate`/`code-review`/`brainstorm`/`plan`: **code-review panel** (agents produce blocking vs non-blocking findings → maps to `signal='done'`), **architecture review** (critique a design doc), **incident review** (root causes + mitigations), **red-team** (proposer / attacker / synthesizer), **research** (independent claim collection, then reconcile). Pairs with the "code review mode" skill row and the moderator/judge row. |
| Low | Enhancement | Richer persona system (quality metadata, versioning, import validation, test-run) | **(2026-06-30 analysis)** Next level for personas: typed **quality fields** (style, domain, adversarial level, verbosity, citation preference) on the card schema; **persona versioning** so exports/zip bundles can record exactly which version ran; a **bulk-import validation report** shown before any DB write (instead of writing then discovering malformed cards); and a **persona test-run** ("try this persona against this topic") for quick iteration. Homepage/conversation persona **preview cards** also fit here (the conversation-page Cast panel already shipped). |
| Low | Enhancement | Model / CLI comparison dashboard | **(2026-06-30 analysis)** Aggregate per-CLI behaviour across conversations: response latency, message length, how often each CLI `blocked` vs completed, turn counts, rough token estimates. Turns the conversation corpus into a "which CLI behaves how" lab view. Soft-depends on the per-conversation stats panel row. |
| Low | Enhancement | Replay mode | **(2026-06-30 analysis)** Re-run the same topic + persona cast with a different CLI mix (or different personas) to compare outcomes head-to-head. Reuses `seed_conversation()` + the Phase 2b spawn wrapper; just needs a "replay this conversation with cast X / CLIs Y" entrypoint that copies topic/preset/personas into a fresh seed. |
| Low | Enhancement | Webhook on conversation completion | **(2026-06-30 analysis)** Fire a configurable webhook (URL + optional bearer) when a conversation flips to `status='complete'`, posting `{conversation_id, topic, participants, end_reason}`, so external tools can archive, summarize, or notify on finished runs. Off by default. |
| Low | Enhancement | Ops / admin page + backup/restore | **(2026-06-30 analysis)** A local-only admin view surfacing sync status (last sidecar tick from `db/.sync-state.json`), DB size, schema version, and conversation/message/persona counts — plus a backup/restore path for the Fly volume and local `db/chat.db`. Overlaps the `/healthz` route in the dependency/deploy-hygiene row (share the DB/schema probe). |
| Low | Enhancement | Wire the Agent Skills into Kimi + OpenCode (if supported) | `scripts/setup/setup-skill-links.ps1` / `.sh` link the repo-root `skills/` (`agent-chat`, `debate-mode`, `start-debate`) into claude-code / codex / gemini / antigravity config dirs only — Kimi and OpenCode were added later and aren't covered. Confirm whether each reads the [Agent Skills](https://developers.openai.com/codex/skills) `SKILL.md` standard (and from which dir — Kimi `.kimi-code/`?, OpenCode `.opencode/`?). If yes: add their skill dirs to the `$cliSkillDirs` list in both setup scripts and the per-CLI path tables in `skills/*/README.md`. If no: note the gap in those READMEs so it's clear the participation/debate skills don't apply to those two CLIs. |
| Medium | Bug fix | `inspect_conversations.py tail` prints "(conversation complete)" prematurely | Observed during smoke test: `tail` exits with that message while `conversations.status` is still `active`. Appears to use "no new messages within poll window" as exit condition. Fix: query the conversation row before exiting; only print `(conversation complete)` when `status='complete'`. Otherwise stay polling or print `(no activity — Ctrl-C to exit)`. |
| Low | Enhancement | Helper scripts under `scripts/` (remaining wrappers) | `scripts/start.ps1` covers seed-conversation + sidecar lifecycle (see Done row 2026-05-06). Still open: `tail-conversation.ps1`, `list-conversations.ps1`, `stop-conversation.ps1` so the read/inspect/stop verbs stop being copy-paste from README. Cross-platform `.sh` siblings if/when relevant. |
| Low | Enhancement | Lightweight CI workflow | GitHub Actions on push: create venv, `pip install -r requirements.txt`, validate `agents/CLIs/claude-code_agent1/.mcp.json` parses as JSON, run `python -c "import sys; sys.path.insert(0, 'src'); import agent_chat_mcp"`. Cheap, catches the obvious regressions. **Once the High "pytest smoke suite" row lands, fold `pytest` + `compileall` into this same workflow.** |
| Low | Enhancement | Multi-conversation and 3+ agent support | Schema and `participants` JSON column already support both. Server side: `get_latest_conversation` always grabs the newest matching row, which prevents two simultaneous conversations involving the same agent. Decide on selection semantics (most recent? explicit `--conversation-id`?) and exercise three-participant turn rotation. **(2026-06-30 analysis)** Concrete shape: add optional `conversation_id` to `get_my_turn` / `wait_for_turn` / `send_message` / `get_conversation_status`, have `get_kickoff()` return the seeded conversation id in its instructions, and keep "latest matching conversation" only as a backward-compatible fallback. |
| Low | Bug fix | `.mcp.json` indentation is inconsistent | In `agents/CLIs/claude-code_agent1/.mcp.json` server entries sit at 4 / 6 / 6 spaces and the closing braces are misaligned (only one `}` where there should be two — file currently parses by luck). Run it through `python -m json.tool` or Prettier and check the result back in. |
| Low | Tech debt | Repo-path duplication across configs and docs | After the 2026-05-12 launcher + `--db-path` defaulting work, only **one** absolute path remains in each MCP config: the launcher itself (`<repo>/scripts/run-mcp-server.ps1`). Still hardcoded in: `agents/CLIs/claude-code_agent1/.mcp.json`, `C:\Users\mikes\.codex\config.toml` (global, outside repo), `agents/CLIs/gemini_agent1/.gemini/settings.json`. Closing this fully would need either env-var substitution per CLI (option (b) from the original venv-portability item — depends on each loader supporting `${VAR}` expansion in `command`/`args`) or a `claude mcp add` / equivalent registration command that resolves the repo at install time. Marginal value at the current scale; bigger if onboarding a second machine. |
| Medium | Enhancement | Survey and add other CLI agents | **OpenCode shipped 2026-06-24 (see Done); Kimi shipped 2026-06-23 (see CHANGELOG).** Both are first-class agents now, documented as per-CLI deep-dives under `docs/CLI-MCP-Config/Per-CLI/` (the "single matrix" idea was superseded by that per-CLI doc set). **Still open:** evaluate Cursor agent, Aider, Continue.dev, and Ollama-backed local agents. For each: confirm MCP server support, document the registration block, run a smoke conversation, capture quirks. |
| Medium | Enhancement | Adopt per-feature documentation pattern (remaining targets) | `docs/App/web-ui.md` shipped 2026-05-06 (see Done). Still pending: `docs/App/wait-for-turn.md` (the long-poll tool + agent loop), `docs/App/conversations.md` (turn / continuous modes, signals, max_turns, schema), `docs/App/kickoff-prompts.md` (linking out to `prompts/Kickoff/kickoff.md` plus authoring guidance). README stays as the entry point; each section links into the deeper doc. Filed as the user's standing principle for future feature work. |
| Low | Enhancement | Skill: "code review mode" prompting style | Companion to debate-mode for code-review style conversations: reference specific lines, distinguish blocking from suggestion comments, end with an explicit approve/request-changes signal that maps cleanly to `signal='done'`. |
| Medium | Enhancement | Orchestrator: sidecar awareness (warning + optional auto-start) | Phase 2a gap surfaced 2026-05-15: `/orchestrate` seeds rows to local `db/chat.db` but does **not** ensure the `db_sync.py` sidecar is running, so operators wonder why their freshly-seeded conversation isn't showing up at `agent-chat.mikesailab.com`. `scripts/start.ps1` already handles this for the CLI path. Two-tier fix: **(a)** cheap first pass — `GET /orchestrate` (and the conversation detail page, post-seed) detects sidecar liveness via `db/.sync-state.json` mtime (≤30s = healthy; older or missing = down) and surfaces a small banner (`"Hosted mirror not syncing — run scripts/start.ps1 -SidecarOnly to bring it up."`) with a copy-paste command. Same banner on the "Next: launch each CLI" panel below the participant list. **(b)** richer: `POST /api/orchestrate` calls into a slim `scripts/sidecar-ensure.ps1` (extract the sidecar-spawn logic out of `start.ps1` so both call sites share it), respecting the same single-launcher detection. Response then includes a `sidecar_started: true \| false \| already_running` field that the form's success-redirect JS surfaces as a toast on the conversation page. Architecture call: probably do (a) first since it's purely defensive and doesn't add spawn complexity, then graduate to (b) once Phase 2b ships (which already needs to spawn CLIs via a wrapper — the sidecar spawn rides along on the same machinery). Hard depends on: nothing — (a) can ship today. Soft depends on: Phase 2b's spawn wrapper extraction for the (b) tier. |
| High | Enhancement | **Ultimate goal — Phase 2b**: personality bundle picker + PowerShell spawn wrapper | Phase 2a shipped 2026-05-15 (`/orchestrate` form + per-CLI preflight + DB row creation; see Done). **A standalone CLI form of the spawn wrapper + personality picker shipped 2026-06-16 as `scripts/debate.ps1`** (see Done) — random topic + random personas + seed + auto-spawn one terminal per CLI in character, persona injected via per-agent prompt file. Remaining Phase 2b work is the **web-form integration** of that capability + a moderator/host mode: (1) **personality bundle picker** in the form — scan `agents/Debate-Agents/` (`Unique-Personas`, `Debate-Hosts`, plus any curated subset folders — discovered dynamically via `personas.discover_groups()`), expose a per-agent dropdown so each spawned CLI gets a specific personality, weave the picked personality into the rendered kickoff template (probably via a `{{PERSONALITY}}` substitution or a per-agent system message); (2) **PowerShell spawn wrapper** (`scripts/orchestrate-debate.ps1`) called from the Python `POST /api/orchestrate` handler after seeding — spawns each CLI in its own terminal window via `Start-Process -WindowStyle Normal`, passes the agent's personality file path + a one-line opening prompt that calls `get_kickoff()`. `.sh` sibling for POSIX. The orchestrator's success-path response then includes the spawned PIDs and the operator just lands on `/conversations/<id>` watching the live transcript fill in. (3) **Optional moderator/host** — third "spawn" path that loads a `Hosts/` personality and runs in `continuous` mode rather than the regular turn rotation. **Hard depends on:** Phase 2a (shipped). **Soft depends on:** survey-other-CLIs row (each new CLI added to the matrix needs its own preflight branch + spawn-arg branch). Open architecture call: which terminal host does `Start-Process` target — Windows Terminal (`wt.exe -w 0 nt`) for nicer UX vs default `conhost` for max compatibility? Probably make it configurable via env var with a sensible default. |
| Medium | Enhancement | Integrate agent personalities into UI / logic / functionality (registry + MCP tools shipped) | **Shipped 2026-06-20 (see Done): option (b) agent-facing MCP self-service** (`list_personas` / `get_persona` tools) **and option (d) the shared persona registry** (`src/orchestrator/personas.py`). An agent can now browse the roster and adopt a card itself, no operator step. **Also shipped 2026-06-20:** `scripts/debate.ps1` now casts from the shared registry (via its JSON CLI) instead of its own folder scan — one source of truth for persona discovery + naming. **Shipped 2026-06-22 (see Done): (c) web UI display** — Cast panel + per-message persona labels on `/conversations/<id>`, fed by the `participant_personas` column; **plus persona CRUD** (`/personas` management page + write layer). **Still open:** (a) **web-form picker** — per-agent personality dropdown in `/orchestrate` (overlaps the Phase 2b row below; the picker can now read `orchestrator.personas` instead of re-scanning) and weaving the chosen card into the rendered kickoff. **Soft depends on / overlaps:** Phase 2b row below (web picker), Debate-Hosts/ → moderator mode. _Original full write-up of the option menu:_ Make the personality cards in `agents/Debate-Agents/` (`Unique-Personas/` = 25 personas, `Debate-Hosts/` = 4 moderators; each a Markdown card with frontmatter + `Purpose`/`Instructions`/`Output Format`/`Example`) a first-class, easily-pickable part of the product rather than just files on disk. **Goal:** an easy way for an agent to "pick up" a personality card and run in character for a debate. **Current state:** cards are already pre-copied into each CLI's native subagent folder (`agents/CLIs/*/.{claude,codex,gemini}/agents/`), and `scripts/debate.ps1` already injects a *random* persona per CLI at launch via per-agent prompt files (`db/launch/conv<id>-<cli>.txt`). So the raw capability exists at the CLI layer — what's missing is (1) **operator-facing selection** and (2) **agent-facing self-service**. **Possible solutions to evaluate (the user is explicitly curious about better options):** (a) **Web-form picker** — per-agent personality dropdown in `/orchestrate`, scanning `agents/Debate-Agents/{Unique-Personas,Debate-Hosts}/`, weaving the chosen card into the rendered kickoff (the `{{PERSONALITY}}` substitution already sketched in the Phase 2b row below — **these two rows overlap; consolidate or cross-reference when one ships**). (b) **MCP self-service tools** — `list_personas()` + `get_persona(name)` (read-mostly, mirroring `get_kickoff`) so an agent can browse the roster and adopt a card itself mid-setup, no operator step. This is arguably the cleanest "agent picks up a card" answer and is reusable across all three CLIs without per-CLI copying. (c) **Web UI display** — surface the assigned persona (name/avatar/one-liner) on `/conversations/<id>` so viewers know who's who; pull from a new `participants`-adjacent column or the kickoff template. (d) **Persona registry** — a small parser (`orchestrator/personas.py`?) that reads the card frontmatter into a typed list, shared by the web picker, the MCP tools, and `debate.ps1` (which currently re-implements its own scan) so there's one source of truth. **Open architecture call:** keep cards as the canonical source and generate everything else from them, vs. promote a DB-backed persona table. **Soft depends on / overlaps:** Phase 2b row below (web picker), Hosts/ → moderator mode. |
| Medium | Enhancement | Web UI: download conversation as JSON / TXT (Markdown + .zip bundle shipped) | **Markdown export shipped** 2026-05-06 (`GET …/export.md`); **comprehensive `.zip` bundle shipped** 2026-06-22 (`GET …/export.zip` → `topic.md` + `personas/<agent>.md` + `transcript.md`, all Markdown; "Download .zip" button; see Done). Remaining: JSON (conversation row + messages array) and plain-TXT transcript formats — fold into a small "Download" dropdown alongside the existing two buttons. |
| Medium | Enhancement | Web UI: shareable read-only conversation links | Generate a per-conversation share token (random URL-safe slug stored in a new `share_token` column). `GET /share/<token>` renders a stripped-down read-only view — no nav chrome, no force-stop button, no edit affordances — suitable for emailing or pasting into Slack. Default off; enable via a "Share" button on the detail page. Note: still local-only; "share" means within the LAN unless the user is tunneling. |
| Medium | Enhancement | Favicon and basic site polish | Add a `/static/favicon.ico` (and a 32x32 PNG variant) plus `<link rel="icon">` tags so browser tabs aren't displaying the default globe. While in there: a real `<title>` per route (`Agent-chat — #{id}: {topic}` for detail, `Agent-chat — Conversations` for list), and an Open Graph block for share-link previews. |
| Medium | Enhancement | Web UI: search across conversations | **Topic/id/participant search shipped 2026-06-29** (see Done): the new `/conversations` rail has a client-side substring filter over the conversation list. **Still open:** full-text search across message **content** (the rail filter only matches list metadata, not message bodies) — SQLite FTS5 virtual table is the clean solution; a `LIKE '%q%'` query is the 10-line version. |
| Medium | Enhancement | Web UI: filter and sort on conversations table | **Partly addressed 2026-06-29** (see Done): the flat table is gone — `/conversations` is now a searchable two-pane rail. **Still open:** explicit sort (newest / oldest / most-messages / status) and filters by status (`active` / `complete`) and by participant. |
| Medium | Enhancement | Web UI: live "whose turn" indicator | On the detail page header, show a "🟢 codex thinking…" badge when the conversation is active and `current_turn` is set. Updates over the same SSE channel that delivers messages. Closes the "is anything happening" gap when both agents are mid-loop. |
| Medium | Enhancement | Web UI: per-conversation stats panel | Sidebar on the detail page: messages-per-agent count, average message length, conversation duration (created → last message), rough token estimate (chars / 4). Quick eyeball on whether one agent is dominating or whether to raise/lower `--max-turns`. |
| Low | Enhancement | Web UI: dark mode | CSS-variables-based theme with a system-preference default and a manual toggle persisted in `localStorage`. Cosmetic but immediately noticeable. |
| Low | Enhancement | Web UI: conversation tags / labels | Add a `tags` column (JSON array). Free-form labels like `experiment`, `archived`, `demo`. Filterable from the list view. Lets the user keep stale conversations without `list` becoming visually noisy. |
| Low | Enhancement | Web UI: editable topic (delete shipped 2026-05-06) | Delete-conversation button shipped via the bidirectional-sync work — × icon per row on `/conversations` with confirm() naming the topic + message count, cascades messages, propagates to the local DB on the next pull tick. **Still open:** pencil icon to rename a topic (typos happen). Edit-topic ride-along is small now that bidirectional sync handles the round-trip — just a `PATCH /api/conversations/{cid}` (or `POST /api/conversations/{cid}/edit`) that updates `topic` + bumps `updated_at`, plus an inline-edit affordance on the conversation detail page header. |
| Low | Enhancement | Web UI: per-message permalinks and copy buttons | Each message gets `#msg-<id>` as a stable anchor and a small "copy link" / "copy text" button on hover. Makes it trivial to point someone at a specific exchange in a long transcript. |
| Low | Enhancement | Web UI: keyboard shortcuts | `j` / `k` to navigate between messages on the detail page, `g` then `c` to jump to the conversations list, `/` to focus search, `?` to show a shortcut help overlay. Cheap once the basics are in. |
| Low | Enhancement | Web UI: empty- and error-state polish | **Empty states shipped 2026-06-29** (see Done): the `/conversations` content pane shows a "select a conversation" empty state, the rail shows "No conversations yet" when the DB is empty, and `/personas` has empty/filtered states. **Still open:** the zero-conversations "Seed your first conversation" CTA and a friendly message (instead of a Starlette stack trace) when a conversation id 404s. |
| Low | Enhancement | Export filename: word-boundary slug truncation | `_topic_slug` in `src/web_ui.py` truncates at exactly 25 characters, which can cut mid-word — e.g. "Should AI agents have memory that persists across sessions?" → `should-ai-agents-have-mem`. Refinement: after the 25-char trim, scan for the last `-` and cut there instead, so the slug ends on a word boundary (would yield `should-ai-agents-have` for the example above). Add a minimum-length guard (e.g. only break at hyphens past index 8) so single-long-word topics don't degenerate into tiny stubs. Trigger when real topics start producing visibly mangled filenames. |

## Done

| Closed | Item | Detail |
|:---|:---|:---|
| 2026-06-30 | Hosted public mirror is read-only (close the public write surface) | Closed the highest-priority finding from the 2026-06-30 repo/site analysis: the Fly mirror exposed unauthenticated browser **write** routes (conversation stop/delete, orchestrate, all persona writes) because `_build_middleware()` returned `[]` unconditionally. **New `ReadOnlyMiddleware` in `src/web_ui.py`** rejects any non-`GET`/`HEAD`/`OPTIONS` request with `403 read-only deployment` — keyed off the HTTP **method**, so new mutation routes are covered automatically — exempting only the bearer-gated `/api/ingest` sync realm (so the local→Fly sidecar keeps pushing). Gated by a default-off env flag `AGENT_CHAT_PUBLIC_READONLY` (`1`/`true`/`yes`/`on`, parsed by new `_env_truthy()`); **`fly.toml` `[env]` sets it for the deploy**, local dev stays fully writable + unauthenticated. **`_build_middleware()` re-wired** to assemble the stack from env flags and **re-enable the dormant `BasicAuthMiddleware`** (attaches when `AGENT_CHAT_BASIC_AUTH_PASSWORD` is set; basic-auth outermost, then read-only). **First tests in the repo:** `tests/test_web_readonly.py` — 10 cases (isolated middleware behaviour, `_env_truthy`, `_build_middleware` wiring across all four flag combos, end-to-end pass over the real route table with an isolated temp DB), pytest-compatible **and** standalone-runnable with the venv (no pytest dep needed). Docs: `web-ui.md` Auth section rewritten + CHANGELOG. Narrows the original High security row to the remaining "real auth + CSRF for *authenticated* hosted writes" follow-up (now Medium). Validated locally (10/10 tests + clean import); **takes effect on the hosted mirror after the next `fly deploy`** (the `fly.toml` env change ships with it). |
| 2026-06-29 | Web UI redesign: persona + conversations consoles + site-wide emerald retheme | Three styling/structure changes in `src/web_ui.py` (no API / schema / route / endpoint changes), deployed to Fly (version 32) and verified live. **(1) `/personas`** → three-pane master-detail console (group rail / persona list / live edit+preview with Edit/Preview markdown tabs), replacing the single-column accordion; search, Name A–Z/Z–A sort, monogram avatars, hover row actions (edit / duplicate / delete), bulk-select, and import all reuse the existing `/api/personas*` endpoints; collapses to a slide-over drawer ≤900px. **(2) Site-wide retheme** → a single emerald accent (`#10b981`) + JetBrains Mono / IBM Plex Sans / IBM Plex Mono across homepage, conversations, transcript, and orchestrate (was sky-400 + Inter); re-aligns the app with the always-emerald favicon. **(3) `/conversations`** → two-pane console (rail of conversations + content pane), replacing the flat table; selecting a row is a plain nav to `/conversations/{id}` that re-renders with the same rail, so live SSE / export / stop / cast panel / highlight.js are untouched (auto-scroll now targets `#cv-main`); rail has client-side topic/id/participant search, per-item delete, and an empty state. Docs: `docs/App/web-ui.md` (personas / conversations / transcript sections) + `docs/CHANGELOG.md`. Partially advances three Open rows (search, filter/sort, empty-state polish) — see their annotations. |
| 2026-06-24 | Add OpenCode CLI as a sixth first-class agent + `debate.ps1` `-Cli` selector + DB-persona fix | Wired [OpenCode](https://opencode.ai) (agent-id `opencode`) through every orchestrator surface, mirroring the Kimi/Antigravity additions: `src/orchestrator/preflight.py` (`opencode` in `SUPPORTED_CLIS`, new `check_opencode()`, `_CHECKS`), `src/web_ui.py` (`/orchestrate` checkbox + badge), tester workspace `agents/CLIs/opencode_agent1/` (`AGENTS.md` + `opencode.json`), `scripts/debate.ps1` (`$Clis` launch row, `-Agents` up to 5), and docs (`docs/CLI-MCP-Config/Per-CLI/opencode.md` + hub row + README + start-new-chat + CHANGELOG). **Key quirk:** OpenCode's MCP config shape differs from the other CLIs — servers under an `mcp` key (not `mcpServers`), `"type": "local"`, and a single `command` **array** (executable + args combined); `check_opencode()` normalizes that into the command/args pair the shared validator expects. Auto-loads `opencode.json` from the launch dir (merged with `~/.config/opencode/opencode.json`, project wins); headless loop is `opencode run "<prompt>"`; skip-permissions `--dangerously-skip-permissions`; auth `opencode auth login`. **Also shipped alongside:** (1) a `-Cli` selector for `debate.ps1` (force the exact CLI set + order, e.g. `-Cli claude-code,opencode`; first = `--first` speaker); (2) a fix so `debate.ps1` sources persona bodies from the **DB** (the runtime source of truth) instead of reading the card markdown off disk at the registry's synthesized `.path` — the disk read broke after the `agents/Debate-Agents/` cards were reorganized; `New-AgentPrompt` now takes the DB body already fetched into `$personaMap`, decoupling the script from the card folders entirely. Validated live: preflight `ok`, `web_ui` imports, JSON parses, and a 2-agent claude-code vs opencode debate (conv #26/#27) ran to completion with opencode replying in turn over the `agent_chat` MCP server. **Not yet validated:** a live 4-/5-agent run. |
| 2026-06-23 | Canonical persona-card format standard + template | Standardized the persona-card format for authoring/generation. New canonical template `agents/Debate-Agent-Templates/Agent-Personality.md` (+ sibling `README.md`): YAML frontmatter (`title`, block-list `tags`, optional `category`/`subcategory`), `## Purpose` (→ roster summary), a **second-person** `## Persona` body with an extensible starter trait menu (Voice, Debate style, You believe, Intelligence, Strengths, Weaknesses, Decision framework, Favorite topics, You avoid), `## Example lines`, `## Stay in character`. Documented the **two-consumer model** in `docs/App/personas.md` (new "Card format standard" section): the parser reads *only* frontmatter + `## Purpose`; the model reads the *entire body* as its prompt — so write instructions in the second person, and the trait bullets are freeform convention, not schema. Captured three parser gotchas surfaced while writing the template: frontmatter must be at byte 0 (a leading comment silently drops all metadata), `tags` must be a block list (no inline `[a, b]`), and YAML `#` comments aren't stripped. Also swept folder-implies-source confusion out of `scripts/debate.ps1` (help text + empty-roster error now point at the DB group / registry JSON CLI, not the on-disk cards), `docs/Guides/auto-debate.md` (`-Group`), and `README.md` (`list_personas` + repo tree). Behavior unchanged; docs/template only. The existing `Politics/Celebrities/Sports/…` trading-card cards still need conversion to this format before import (they parse with empty tags/category/summary) — tracked informally for the next import pass. |
| 2026-06-22 | DB-backed persona registry — bidirectionally synced, hosted CRUD | Follow-on to the persona-CRUD row below, which shipped **local-only** because the `agents/Debate-Agents/` card tree isn't in the Docker image and Fly's FS is read-only. **Fix:** personas are now first-class data in a `personas` table inside `db/chat.db` (composite PK `("group", slug)`), synced bidirectionally by the existing sidecar — so add/edit/delete works and persists on **both** local and `agent-chat.mikesailab.com`. DB is the runtime source of truth; the `.md` cards become a one-time **import seed** + git snapshot only (no DB→files export — user's call). Rewrote `src/orchestrator/personas.py` read/write to SQLite (`discover_groups`/`list_personas`/`get_persona`/`create`/`update`/`delete` all DB-backed; `root_exists()` now = "DB reachable", un-gating hosted management; new `import_personas_from_files()` + `personas.py import [--overwrite]` CLI). Mirrored the conversation sync machinery for the `personas` table in `scripts/db_sync.py` (`PERSONA_COLUMNS`, `read_changed_personas`, `read_all_persona_keys`, three new watermarks, persona upsert/delete in `apply_pull`/`run_tick`) and `src/web_ui.py` (`ingest_payload`/`since_payload`/`api_ingest`/`api_since` extended). Composite key serialized on the wire as `group␟slug` (ASCII US `0x1F`). Schema added to all three `SCHEMA` constants. Web UI un-gated (copy + 404 messages updated). Backward-compatible both mixed-version directions. Migration: ran `personas.py import` once (29 cards). Validated: registry CRUD incl. group-move + collision, importer idempotency, `debate.ps1 -DryRun` parity, an end-to-end local↔remote sync sim (push/pull/delete both ways), and an HTTP-level `TestClient` round-trip (`\x1f` survives JSON + querystring; old-sidecar back-compat). Docs: `personas.md`, `db-sync.md`, `web-ui.md`, CHANGELOG. |
| 2026-06-22 | Persona cast on the conversation page + persona CRUD in the web UI | **Option (c) shipped** from the "integrate agent personalities" row: the conversation detail page renders a **Cast** panel (from `participant_personas`) — expandable per-participant entries (CLI tool + persona name → full card) — and labels every message header with the persona name (server-rendered + live SSE, via a `PERSONAS` JS map); `_CAST_CSS` added. **Plus persona CRUD:** new `GET /personas` management page (top-nav link) with add form + per-card edit/delete, backed by `POST /api/personas`, `POST /api/personas/{slug}`, `POST /api/personas/{slug}/delete`. Registry gained a write layer (`create_persona`/`update_persona`/`delete_persona`/`slugify`/`_serialize_card`/`root_exists`/`_find_persona_any_group`). **Local-only** (gated on `root_exists()`; hosted mirror shows an "unavailable" notice + endpoints `404`, since `agents/` isn't deployed). Gotchas documented (sidecar restart after `db_sync.py` edits; transient `fly deploy` retry). Validated: write round-trip incl. group-move, page render, HTTP smoke (page + create + delete), imports. |
| 2026-06-22 | Comprehensive conversation export (.zip bundle) + persisted persona cast | New `GET /api/conversations/{cid}/export.zip` returns a Markdown bundle — `topic.md` (topic + overview metadata + kickoff framing), `personas/<agent>-<slug>.md` (one per participant: CLI tool + full personality card), `transcript.md` (the full debate). "Download .zip" button beside "Export Markdown". Enabled by a **new `conversations.participant_personas` JSON column** (`{agent_id: {persona_slug, persona_name, persona_body}}`) — the full card body is stored so the bundle is complete on the hosted mirror too (the `agents/` cards aren't deployed). Wired through `SCHEMA`/`_MIGRATIONS` (agent_chat_mcp.py, web_ui.py, seeding.py) + sync column lists (`web_ui._CONV_COLUMNS`, `db_sync.CONV_COLUMNS`). Populated by `seed_conversation(participant_personas=…)` ← `start_conversation.py --participant-personas-file` ← `scripts/debate.ps1` (captures tool+persona+body from the registry at launch). Conversations seeded without a cast still export (persona doc notes "not recorded"). Validated: column store/read, CLI flag, zip render (3 parts), no-persona degrade, `debate.ps1` parse + dry-run, live DB migrated. **Progresses** the "integrate agent personalities" row's option (c) — the persona cast is now persisted per conversation, so surfacing it on `/conversations/<id>` is just a render step. JSON/TXT export formats remain open (own row). |
| 2026-06-22 | Curated persona subgroups + persona-folder rename (`debate.ps1 -Group`) | The persona registry (`src/orchestrator/personas.py`) now discovers groups dynamically: `discover_groups()` enumerates every subfolder of `agents/Debate-Agents/`, with `PREFERRED_GROUPS = ("Unique-Personas", "Debate-Hosts")` sorting first / forming the canonical roster when no group is requested. Any other subfolder (e.g. `Crypto-Panel/`) is a valid curated-subset group — drop in `*.md` cards, no code change (replaces the never-implemented `Group1/2/3` idea). `scripts/debate.ps1` gained `-Group <name>` (default `Unique-Personas`), which also scopes `-Personalities`. **Folder rename handled:** the operator renamed `All/` → `Unique-Personas/` (25 debater cards) and `Hosts/` → `Debate-Hosts/` (4 moderator cards); `PREFERRED_GROUPS`, `DEFAULT_DEBATER_GROUP`, the `-Group` default, all MCP tool docstrings/field descriptions, and every doc reference were updated to match (without which the canonical roster + default cast resolved to zero personas). Validated: registry import (29 cards, Unique-Personas sorts first), `debate.ps1` parse + default-group dry run, and a `-Group` dry run drawing only from a throwaway curated folder. |
| 2026-06-22 | Shared Agent Skills linked into every CLI workspace (`scripts/setup/setup-skill-links.*`) | New `scripts/setup/setup-skill-links.ps1` (Windows directory junctions) + `.sh` (POSIX symlinks), modelled on the AI-Automation-Library pattern, wire the canonical repo-root `skills/` (`agent-chat`, `debate-mode`) into each CLI tester workspace's own **gitignored** config dir — claude-code→`.claude/skills`, codex→`.codex/skills`, gemini→`.gemini/skills`, antigravity→`.agents/skills`. Idempotent, run once per clone; refuses to clobber real (non-link) dirs. Removed two stale hand-copied `agent-chat` skill folders (codex/gemini, dated May 15) that predated the persona tools. `skills/*/README.md` reworked to make the setup script the recommended install path and to add Antigravity (Gemini marked deprecated fallback). New step added to `docs/Setup/INITIAL_SETUP.md`. Validated: all 8 junctions resolve to canonical `skills/`, none appear in `git status`. |
| 2026-06-20 | Add Antigravity CLI — `debate.ps1` auto-spawn row (finishes the migration) | Closed the last open piece of the Antigravity migration: the one-command auto-debate launcher can now spawn Antigravity in a terminal. **`scripts/debate.ps1`** — the `$Clis` launch table's `gemini` row was replaced by `antigravity` (`Dir = agents\CLIs\antigravity_agent1`, `Exe = agy`, `PromptArg = -i {0}`, `SkipPerm = --dangerously-skip-permissions`), so the CLI preference order is now `claude-code → antigravity → codex` (2-debater = claude-code + antigravity; 3 = + codex). Stale `gemini` references in the script's comment-based help were swept (the `.DESCRIPTION` CLI-order line + the `-SkipPermissions` param note, which had referenced a non-existent `$CliSkipPerm` and claimed only claude-code was wired). **Headless invocation resolved** (was the blocker): binary `agy` on PATH, initial-prompt flag `-i "<prompt>"`, skip-approval flag `--dangerously-skip-permissions` (plus `toolPermission`/`artifactReviewPolicy: always-proceed` in `.agents/settings.json` as the persistent alternative) — all documented in `docs/CLI-MCP-Config/antigravity.md` ("Configuration Details & Auto-Spawn Support" section, replacing the placeholder "Known quirks"). **Bug fix:** `src/orchestrator/preflight.py:check_antigravity()` was reading `.agents/mcp.json`, but the file Antigravity actually loads (and the one on disk) is `.agents/mcp_config.json` — the `/orchestrate` antigravity badge would have falsely reported `config_missing`. Fixed the path; preflight now returns `ok=True`. **Workspace scaffold** (`package.json`, `src/AGENTS.md`, inner `.gitignore`) was already pruned in the prior migration — only `AGENTS.md` + `.agents/` remain, matching the tester role. Docs swept: `docs/Guides/auto-debate.md` (prereqs, flag note, debater-count mapping, persona-order, example log block all `gemini → antigravity`). Validated: `check_antigravity()` `ok=True`, `debate.ps1` parses cleanly, and a `-DryRun -Agents 2 -SkipPermissions` produced the correct `agy --dangerously-skip-permissions -i "..."` launch plan from the antigravity workspace folder. |
| 2026-06-20 | Add Antigravity CLI as a supported agent (additive; Gemini kept as fallback) | Google deprecated the Gemini CLI → wired its successor **Antigravity** (agent-id `antigravity`, workspace `agents/CLIs/antigravity_agent1/`) as a first-class agent without removing Gemini. Shipped: `agent_chat` MCP block added to `.agents/mcp_config.json` (→ `run-mcp-server.ps1 antigravity`); `AGENTS.md` rewritten from the copied Gemini doc to be Antigravity-specific (agent-id, `.agents/mcp_config.json` config path, doc refs, successor note); `src/orchestrator/preflight.py` gained `check_antigravity()` (reads `.agents/mcp_config.json`) + `antigravity` in `SUPPORTED_CLIS`/`_CHECKS` (preflight passes); `src/web_ui.py` `/orchestrate` form gained an `antigravity` checkbox + status badge (Gemini relabeled "deprecated"); new `docs/CLI-MCP-Config/antigravity.md` (mirrors `gemini.md`) + README CLI references (config collapsible, repo tree, docs index). **Security:** `.agents/mcp_config.json` is tracked (the `agent_chat` config), but its Serper key was switched to `${SERPER_API_KEY}` so no secret enters the repo; the rest of `.agents/` stays local via `.gitignore`, and the workspace-scaffold `package.json`/`src/` were pruned. Validated: mcp_config.json parses, preflight `ok=True` for antigravity, `/orchestrate` renders 200 with all four CLI checkboxes. Remaining (own Open row): the `scripts/debate.ps1` auto-spawn launch row, blocked on the Antigravity CLI's headless invocation details. |
| 2026-06-20 | Persona registry + `list_personas` / `get_persona` MCP tools | First slice of the "integrate agent personalities" item — options (b) + (d). New `src/orchestrator/personas.py` reads the cards under `agents/Debate-Agents/` (`All/` = 25 debaters, `Hosts/` = 4 moderators) into a typed list (`Persona` dataclass; `list_personas(group=None)`, `get_persona(query)` matching slug-or-display-name, case/punctuation/emoji-insensitive). Stdlib-only — hand-rolled frontmatter parser, no PyYAML added. Two read-only MCP tools in `agent_chat_mcp.py` mirror `get_kickoff`'s annotations: `list_personas` (lightweight roster — `slug`/`name`/`group`/`tags`/`summary`, optional `group` filter) and `get_persona(name)` (full card body as `instructions`, or `{status:"not_found", available:[…]}`). An agent can now browse the roster and adopt a card itself — no operator step, no per-CLI file copying. Validated: import smoke test, FastMCP registers both tools, all 29 cards load, slug/name/quoted-name lookups resolve, valid JSON out. **Also folded `scripts/debate.ps1` onto the registry the same day:** added a JSON CLI to `personas.py` (`python src/orchestrator/personas.py {list,get} --group All`, ASCII-safe output) and rewrote `debate.ps1`'s persona selection to call it — deleting the `Get-PersonaName` helper + folder glob, and gaining slug-or-display-name resolution for `-Personalities`. Validated via `-DryRun` (forced-by-slug, forced-by-name, random 3-agent, not-found). Docs swept: `docs/App/personas.md`, `docs/Guides/auto-debate.md`, CHANGELOG. Narrows the still-open personality row to (a) web-form picker + (c) web UI persona display. |
| 2026-06-20 | Run a 3-agent conversation (claude-code + codex + gemini) | Three-way turn rotation validated end-to-end. Gemini's MCP registration (`docs/CLI-MCP-Config/gemini.md`) + tester role doc (`agents/CLIs/gemini_agent1/GEMINI.md`) were already in place; the `.gemini/settings.json` snippet was pasted and a 3-agent `turns` conversation run to completion with all three CLIs taking turns in order. Confirms three-participant rotation works against the existing schema/`participants` column. (Residual `get_latest_conversation` selection-semantics nuance for *simultaneous* conversations sharing an agent remains tracked in the Low "Multi-conversation and 3+ agent support" Open row.) |
| 2026-06-16 | Exercise unexercised completion code paths — `(a)` max-turns | Path **(b)** `signal='done'` was proven 2026-05-06 (conversation #14, Bob Lazar). Path **(a)** natural max-turns completion now confirmed: conversation #22 (auto-debate, 2 agents, `max_turns=8`) flipped to `status='complete'` with `end_reason = "max_turns reached (8 per agent)"` — and several earlier runs (#5, #6, #16) hit the same path. Both completion paths now exercised end-to-end. |
| 2026-06-16 | Auto-debate launcher — `scripts/debate.ps1` (Phase 2b, CLI form) | One-command hands-off debate spawner. Picks a random **unused** topic from `docs/Chat-Topics/Topics.md`, reads that topic's `- Debaters: N` line to size the cast (`2` → claude-code + gemini, `3` → + codex), draws N random personas from `agents/Debate-Agents/All/`, maps one per CLI in preference order (first = `--first`), seeds through `scripts/start.ps1 --preset debate` (so the sidecar comes up + topic/preset/kickoff render path is reused — no duplicated seeding logic), captures the new conversation id, then spawns one `pwsh` window per agent that `cd`s into the agent's `agents/CLIs/<name>_agent1/` folder and launches the CLI in character. **Persona injection at launch** (not via `get_kickoff()`, which returns one shared template per conversation): each agent gets a prompt file at `db/launch/conv<id>-<cli>.txt` (persona body + in-character kickoff instructions) and the CLI receives only a tiny "read this file and follow it" opener — persona length/quotes can't break the command line. Windows spawned via `pwsh -EncodedCommand` (base64) to sidestep `Start-Process` quote mangling. **Topic check-off:** on successful seed the chosen topic line gets a ✅ marker appended (`… ✅ <!--used YYYY-MM-DD conv#N-->`); parser skips ✅ topics next run and aborts cleanly when the list is exhausted. **Run history:** appends a topic + persona→CLI cast block to `logs/debate-history.log` per run (the message transcript stays in the DB). Flags: `-DryRun`, `-SkipPermissions` (`claude --dangerously-skip-permissions`, `gemini --yolo`, `codex --yolo`), `-Topic`, `-Agents`, `-Personalities`, `-MaxTurns`, `-DefaultAgents`, `-TopicsGlob`, `-ForceSidecar`. Per-CLI launch details (binary, initial-prompt flag, skip-approval flag, working dir) live in one editable `$Clis` table at the top of the script. Validated end-to-end: conversation #22 (2-debater, claude-code = Crypto Chad, gemini = Chad "Alpha" Chadson) ran the full loop in character to a natural `max_turns` completion, topic checked off, cast logged. New guide `docs/Guides/auto-debate.md`; README repo-layout + docs index updated; Chat-Topics library swapped to `Topics.md` (+ `Legacy/`). Narrows the still-open Phase 2b row to web-form integration + moderator/host mode. |
| 2026-05-15 | Orchestrator Phase 2a — `/orchestrate` form + preflight + DB row creation | Shipped the seed-form + per-CLI preflight half of the ultimate-goal orchestrator. New `src/orchestrator/` package (3 files): `seeding.py` extracted from `start_conversation.py:main()` (pure function, same SCHEMA + _MIGRATIONS constants, raises `SeedError` on validation failure); `preflight.py` with `PreflightResult` + per-CLI checkers (`check_claude_code`, `check_codex`, `check_gemini`) + `run_preflight()` dispatcher + `format_preflight_log()`; `__init__.py` with Phase 2a/2b notes. `start_conversation.py` refactored to a thin argparse wrapper around `seeding.seed_conversation()` (same CLI behaviour, same flags, same exit codes). `src/web_ui.py` gained: `GET /orchestrate` handler running page-load preflight on all three supported CLIs + rendering per-CLI status badges next to each checkbox; `POST /api/orchestrate` handler accepting JSON `{topic, participants, preset, max_turns, first, kickoff}` with strict validation, running preflight on the **selected** CLIs only, aborting with 409 + `{ok: false, kind: "preflight_failed", preflight: [...], log_path: "..."}` on any failure (writing full report to `<repo>/logs/orchestrator-<timestamp>.log`) or returning 200 + `{ok: true, conversation_id: N}` on success; `_render_orchestrate()` HTML render with embedded JS for preset-driven max_turns auto-fill, dynamic first-speaker dropdown, JSON POST submit, inline error rendering, and redirect to `/conversations/<id>` on success; `ORCHESTRATE_CSS` constant scoped under `.orch-shell` reusing BASE_CSS tokens; topbar nav link in `_layout()`. Preflight checks are pure file-system (no subprocess): config file exists at known location, parses (JSON or TOML), has `agent_chat` entry, `command` resolves on PATH or as a file, launcher script referenced in `args` exists. Failure codes form a small enum (`config_missing`, `config_parse_error`, `no_mcp_entry`, `missing_command`, `command_not_found`, `missing_args`, `launcher_not_extractable`, `launcher_missing`, `unknown_cli`) so the UI surfaces a monospace chip + the detail and the log file is `grep FAIL`-able. Smoke-tested via Starlette `TestClient`: form renders with three "ready" badges on the live repo, six validation paths return 400, bogus-CLI preflight failure returns 409 with the right shape and writes a log, happy-path POST creates a conversation row with `preset=debate`, `mode=turns`, `max_turns=2`, kickoff template rendered (1372 chars). Closes the "Web UI: seed-new-conversation form" Open row (was Medium); narrows the "Ultimate goal: orchestrator" Open row to Phase 2b only — personality bundle picker from `agents/Debate-Agents/` + PowerShell wrapper that spawns each CLI in its own terminal + auto-open of the live transcript. |
| 2026-05-15 | `debate-mode` skill — layered on `agent-chat` | Shipped `skills/debate-mode/SKILL.md` (same Agent Skills format as the base skill; loads natively in Claude Code, Codex, and Gemini). Composes on top of `agent-chat`: the base skill governs the `get_kickoff` → `wait_for_turn` → `send_message` loop mechanics, this one shapes the **content** of each reply. Three core teachings, distilled from the `docs/Agent-Conversations/future-of-tech-jobs/Conversation.md` reference debate: (1) argue a position — falsifiable claims and concrete predictions, not surveys; (2) cite the other side specifically — quote or paraphrase the actual argument, engage the strongest version, no strawmen; (3) concede partial points where warranted, hold ground where you can. SKILL.md ships an **anti-patterns table** (the specific filler phrases to avoid — "that's a great point", "valid arguments on both sides", "depends on how we define X", restating-with-more-words) plus a **"phrases that signal good debate"** list lifted from the reference conversation ("Your strongest point is…", "I buy A but I think you understate B", "Concrete bet I'll stake on the table…", "A piece neither of us has named…"). Frontmatter `description` triggers on debate-specific phrases ("argue for X", "defend the position", "kickoff preset: debate") independently of the base skill's triggers — both fire when appropriate, no conflict. SKILL.md includes a **"when to signal `done`"** section that handles three natural ending states (mutual convergence, decisive concession, argument has cycled) with escape tactics for the cycle failure mode. Companion `README.md` covers per-skill install (same per-CLI paths as the base skill, substituting `debate-mode`) plus a **one-symlink-per-machine recipe** that covers both skills at once by symlinking each CLI's skills root directly at this repo's `skills/` folder. Documentation-only change: no new Python deps, no schema changes, no MCP-tool changes. Cross-references updated: README repo-layout tree restructured to show `skills/` with both subfolders, Project-docs table gained a `skills/debate-mode/` row, Roadmap "short-term highlights" rewritten to surface the ultimate-goal orchestrator (now the only High Open item blocked on the seed-conversation form + spawn wiring) as the next item. Closes the "Skill: 'debate mode' prompting style" Open row (was Medium). |
| 2026-05-15 | `agent-chat` Agent Skill — single SKILL.md across Claude Code, Codex, Gemini | Shipped a **role-agnostic** participation skill at `skills/agent-chat/SKILL.md` (committable; the orchestrator can reference it) covering the `get_kickoff` → `wait_for_turn` → `send_message` loop, `signal='done'`/`'blocked'` semantics, turn-taking rules, and the "don't ask the operator between turns" expectation. **One canonical file, three CLIs**: all three target CLIs natively support the open [Agent Skills](https://developers.openai.com/codex/skills) standard (same YAML-frontmatter `SKILL.md` shape, same lazy-load behaviour) — initially mis-shipped as three separate files (`SKILL.md`/`AGENTS.md`/`GEMINI.md`) on the wrong assumption that Codex and Gemini only had instruction-file conventions; corrected after verifying the docs. Per-CLI discovery paths: Claude Code reads `.claude/skills/agent-chat/` (project) or `~/.claude/skills/agent-chat/` (user); Codex reads `$CWD/.agents/skills/agent-chat/` (walks up to repo root) or `~/.agents/skills/agent-chat/`; Gemini reads `.gemini/skills/` or `.agents/skills/` (project) plus the same paths under `~/`. **`.agents/skills/` is recognised by both Codex and Gemini**, so a single symlink at `~/.agents/skills/agent-chat/` covers both CLIs and only Claude Code needs a second install at `~/.claude/skills/agent-chat/`. Companion `README.md` in the same folder covers per-CLI install (copy or symlink), a one-symlink-per-machine recipe, verification (2-turn smoke conversation that should run autonomously from a one-line "join the conversation" prompt), and the intentionally-preserved separation from the tester role docs at `agents/CLIs/*/` — tester docs keep their tester-specific sections ("What to test for", "Reporting") while the skill is canonical for the participation-loop content. Documentation-only change: no new Python deps, no schema changes, no MCP-tool changes. Cross-references updated: README repo-layout tree gained the `skills/` block (between `prompts/` and `agents/`), README Project-docs table gained a `skills/agent-chat/` row, README Roadmap "Recently shipped" + "short-term highlights" sections rewritten to surface the next two roadmap items (debate-mode skill and the ultimate-goal orchestrator). Closes the two Open rows: "`agent-chat` Claude Code skill" (was High) and "Equivalent skills/instructions for Codex and Gemini" (was Medium). |
| 2026-05-12 | Web UI: code block syntax highlighting | highlight.js v11.10.0 wired into the conversation detail page via CDN (`github-dark` theme). Markdown renderer's `class="language-X"` on fenced blocks feeds language hints straight to highlight.js. Scoped to detail page only via a new optional `head_extras` parameter on `_layout()`; homepage + list page don't load the library. SSE-driven live-append path also calls `hljs.highlightElement()` on the newly-inserted node so mid-conversation messages get the same treatment as the initial render. One small CSS override (`.msg-body pre code.hljs { background: transparent }`) so the existing `<pre>` box styling wins over the theme's default. No new Python deps. Smoke-tested end-to-end on a synthetic conversation with both Python and JavaScript fences. |
| 2026-05-12 | Sync stale `get_my_turn` references to `wait_for_turn` / `get_kickoff` | The printed end-of-script message in `src/start_conversation.py` was rewritten in the server-delivered-kickoff feature: it now shows the two-line `get_kickoff()` prompt when a template was rendered, and falls back to "Next: see docs/Guides/start-new-chat.md §3" when not. `docs/Setup/INITIAL_SETUP.md` line 169 ("Ask each agent to call `get_my_turn` and reply via `send_message`...") rewritten to "Ask each agent to call `get_kickoff` once, then drive itself through the `wait_for_turn` → `send_message` loop". The README references the row originally listed (design-notes "no background polling" line, Terminal 2/3 prompt example, "Possible next steps" callout) no longer exist after the homepage redesign — the surviving README references all describe both tools accurately, with `wait_for_turn` as the recommended loop tool. |
| 2026-05-12 | Server-delivered kickoff + presets (top priority) | Replaced the "paste a 30-line prompt into every CLI" workflow with a `get_kickoff()` MCP tool + named presets. **MCP server:** new `@mcp.tool() get_kickoff()` in `src/agent_chat_mcp.py` returns `{status, agent_id, conversation_id, topic, preset, instructions}` for the latest conversation the agent is in; three statuses (`ok` / `fallback` / `no_conversation`); annotations mirror `get_my_turn` (readOnly + idempotent). **Presets:** new `src/presets.py` exposing a `PRESETS` dict — `debate` (turns/8), `code-review` (turns/6), `brainstorm` (continuous/10), `plan` (turns/8) — with tone strings lifted verbatim from `prompts/Kickoff/kickoff.md`'s tone examples. **Seed:** `start_conversation.py` gains `--preset`, `--tone "<override>"`, `--kickoff-template-file <path>`; precedence for mode/max_turns is explicit-flag > preset > script-default; renders the template with `{{TOPIC}}` / `{{TONE_INSTRUCTION}}` substitution + a "with another AI agent" → "with N other AI agents" rewrite for 3+ participant conversations; stores the rendered body in the new `conversations.kickoff_template` column. Template loader supports both Markdown-with-`` ```text ``-fence (default kickoff.md shape) and plain-text custom files. End-of-script message switches to the new two-line prompt when a template was rendered, else points at `start-new-chat.md` §3. **Schema:** added `preset TEXT` + `kickoff_template TEXT` columns to `conversations` via `_MIGRATIONS` constant + idempotent `PRAGMA table_info()` gate in `db_init()` (agent_chat_mcp.py + web_ui.py) and an inline block in `start_conversation.main()`; also added to `web_ui._CONV_COLUMNS` and `scripts/db_sync.CONV_COLUMNS` so new fields round-trip through bidirectional sync. **Docs:** `prompts/Kickoff/kickoff.md` rewritten to lead with the preset/`get_kickoff()` flow and demote the paste-the-prompt workflow to a "Legacy" fallback section; three tester role docs (`claude.md`, `AGENTS.md`, `GEMINI.md`) updated so step 1 of every "How to participate" section is "call `get_kickoff()` once" + tools table gained the new tool; new per-feature doc `docs/App/kickoff-prompts.md` with architecture diagram, CLI flag reference, preset table, rendering-pipeline section, response-shape examples, custom-template authoring, schema-sync surface table, backward-compat story. **Backward compatible** all the way: legacy rows migrate to NULL columns and surface as `status="fallback"`; seeding without the new flags preserves the old end-to-end flow; agents that don't call `get_kickoff()` keep working. **Smoke-tested** via a 6-case in-process script: PRESETS shape, renderer on 2/3/4 agents, legacy-schema DB migration + idempotent re-run, end-to-end seed + `get_kickoff()` returning rendered debate-preset body, fallback path, no-conversation path. Unblocks the still-open "Web UI: seed-new-conversation form" row (can expose the preset dropdown directly). |
| 2026-05-12 | `scripts/run-mcp-server.{ps1,sh}` launcher for portable MCP configs | New launcher script per OS that resolves the venv interpreter (`.venv/Scripts/python.exe` or `.venv/bin/python`) and the MCP server (`src/agent_chat_mcp.py`) relative to its own location (`$PSScriptRoot/..` or `$BASH_SOURCE`). Each CLI config now references just the launcher path — venv interpreter and server-script paths drop out entirely. Cloning to a different drive = edit one absolute path per config instead of two. **Trade-off:** the Windows form requires `pwsh` on PATH (consistent with root `CLAUDE.md`'s "PowerShell 7+" convention). POSIX form uses the `.sh` launcher directly with no host. New `.gitattributes` line `*.sh text eol=lf` keeps the shebang LF-clean across Windows `core.autocrlf`. `.sh` ships with the +x bit set in the git index via `git update-index --chmod=+x`. Doc sweep: README + `docs/CLI-MCP-Config/{claude,codex,gemini}.md` + `docs/Setup/INITIAL_SETUP.md` registration blocks all switched; the codex manual stderr-debug command also rewritten to use the launcher; stale macOS/Linux NOTEs about swapping `.venv\Scripts\python.exe`→`.venv/bin/python` removed (the launcher handles it). Two adjacent doc-accuracy nits caught: `fly-deploy.md` `web_ui.py` `--db-path` description (now has computed `<repo>/db/chat.db` fallback on top of env) + `db-sync.md` troubleshooting tip referencing "path baked into MCP configs" (no longer applicable). Backward compatible — old direct-venv-python configs keep working. Smoke-tested via `pwsh -NoProfile -File scripts\run-mcp-server.ps1 test-launcher --help` (named + positional forms both forward correctly to the Python child). Closes the "Make venv interpreter path portable" Open row; narrows "Repo-path duplication across configs and docs" to just the launcher path itself. |
| 2026-05-12 | `--db-path` default extended to operator scripts + web UI | Same flag → `$AGENT_CHAT_DB` → computed `<repo>/db/chat.db` precedence applied to `src/start_conversation.py`, `src/inspect_conversations.py`, and `src/web_ui.py` (which already had env-var fallback; got the computed default). Each grew its own local `_default_db_path()` helper mirroring the MCP server's pattern. `scripts/start.ps1` needed no code change (it forwards args verbatim) — only the synopsis-comment example was updated. Doc sweep across README (quick-start, daily-driver, inspection), `docs/Guides/start-new-chat.md`, `docs/Setup/INITIAL_SETUP.md`, `docs/CLI-MCP-Config/{claude,codex,gemini}.md` "Run a 3-agent conversation" blocks, `docs/App/web-ui.md` config table + bind example, `prompts/Kickoff/kickoff.md` seed snippet. Validated locally via a 4-case smoke script per entry point + `--help` checks. |
| 2026-05-12 | `--db-path` default-from-env on `agent_chat_mcp.py` | `parse_args()` now makes `--db-path` optional. Resolution in `main()`: flag > `$AGENT_CHAT_DB` > computed `<repo>/db/chat.db` derived from `Path(__file__).resolve().parent.parent`. New `_default_db_path()` helper centralises the env-var-and-default logic; `db_init()` keeps `makedirs`-ing the parent on first run. **Config impact:** the `--db-path` line drops out of every CLI registration. Old configs still pass `--db-path` keep working (flag wins). Updated snippets in `README.md` and `docs/CLI-MCP-Config/{claude,codex,gemini}.md`, plus the manual stderr-debug command in `codex.md`. Module docstring rewritten to show the new minimal `python agent_chat_mcp.py --agent-id claude-code` invocation. Scope intentionally MCP-server-only — `start_conversation.py` / `inspect_conversations.py` / `web_ui.py` still require `--db-path` (web_ui already falls back to `$AGENT_CHAT_DB`). Closes the "`--db-path` default-from-env" Open row; narrows the still-open "Make venv interpreter path portable" and "Repo-path duplication across configs and docs" rows — venv-interpreter + script paths remain hardcoded in MCP configs. |
| 2026-05-06 | Bidirectional DB sync + hosted-UI delete-conversation button | Sidecar now pulls hosted-side conversation deltas via new `GET /api/since` endpoint each tick before pushing local deltas. **Architectural call:** conversations sync both ways; messages stay local-only-origin (agents only run locally; SQLite AUTOINCREMENT collisions would otherwise need UUIDs). Conflict model: last-write-wins by `updated_at`. Mixed-version handling: 404 from `/api/since` → soft `PullNotSupported`, sidecar logs a warning and keeps pushing — new sidecar can talk to old server. New `pulled_updated_at` watermark in `db/.sync-state.json` (backward-compat load); `conversations_updated_after` is bumped to `server_time` after each pull so just-pulled rows don't ping-pong on the next push. New `POST /api/conversations/{cid}/delete` endpoint (cascades messages, idempotent). Hosted UI: × button per row on `/conversations` with confirm() naming topic + message count, removes the row on success. Smoke-tested via `TestClient`: auth, param validation, set-difference deletion computation, delete-with-cascade, idempotent re-delete, and a sidecar round-trip simulation across 4 ticks (initial pull, no-op tick, hosted-side delete propagating to local, mixed-version 404 graceful skip). Docs: `docs/App/db-sync.md` rewritten with the new architecture diagram, conflict model, asymmetry section, mixed-version handling, watermark table; `docs/App/web-ui.md` route map adds both new endpoints; README route table + Public-mirror section updated. Closes the "DB sync: bidirectional (Fly → local)" Open row; narrows the "Web UI: editable topic + delete-conversation button" Open row to edit-topic-only. |
| 2026-05-06 | Folder reorganization: themed subfolders under `docs/` and `agents/` | Top-level docs moved into themed subfolders: `docs/App/` (`web-ui.md`, `db-sync.md`, `fly-deploy.md`), `docs/Setup/` (`INITIAL_SETUP.md`), `docs/Guides/` (`start-new-chat.md`). `docs/clis/` renamed to `docs/CLI-MCP-Config/` and gained `claude.md` + `codex.md` alongside `gemini.md` (closes the separate "Backfill `docs/clis/claude-code.md` + `docs/clis/codex.md`" Open row at the new path). `docs/agent-conversations/` capitalized to `docs/Agent-Conversations/`. New `docs/Chat-Topics/` houses curated topic-prompt libraries. `agents/` per-CLI tester workspaces nested under `agents/CLIs/`; new `agents/Debate-Agents/` with `All` / `Group1` / `Group2` / `Group3` / `Hosts` for personality bundles. Cross-references swept across README, CLAUDE.md, the live homepage's GitHub blob URLs in `src/web_ui.py`, the moved docs themselves (relative paths shifted by depth), `agents/CLIs/gemini_agent1/GEMINI.md`, `fly.toml`, `scripts/db_sync.py`, `scripts/start.ps1`. Memory updated. Historical entries in `CHANGELOG.md` and `Roadmap.md` intentionally not rewritten — they reference paths that were correct at the time of writing. Smoke-tested via `TestClient`: homepage renders with the new GitHub URLs and zero stale-path leaks. |
| 2026-05-06 | Per-CLI MCP-config docs: backfill Claude Code + Codex | `docs/CLI-MCP-Config/{claude,codex,gemini}.md` now all exist with consistent shape: where the config lives, exact registration block, how to verify the server registered, run-a-3-agent-conversation recipe, "Known quirks" section. Closed at the new path under `CLI-MCP-Config/` rather than the original `clis/` (renamed during the same-day folder reorg above). |
| 2026-05-06 | Public landing page at `GET /` + `docs/web-ui.md` | New homepage at `agent-chat.mikesailab.com/` (formerly the conversations table, which moved to `/conversations`). Self-contained "console-arena" design — near-black canvas (`#07090a`), single emerald accent (`#10b981`, matches the favicon), heavy JetBrains Mono display + IBM Plex Sans body + IBM Plex Mono code, SVG noise grain + dual emerald radial spotlights, one staggered reveal on page load. Sections: topbar with live-pill, asymmetric hero (oversized two-row title, lede, dual CTAs, stats panel from `list_stats()`), three "what" cards, five numbered "how" steps with code, latest-5 conversations, six-group resources grid (this project · prompt library — including the [Agents page](https://prompts.mikesailab.com/?library=public&section=agents) · archived debates · stack · CLIs · author), monospaced footer. New `list_stats()` helper (three indexed `COUNT(*)` queries) feeds live counters and the topbar `N live` pill. Brand link in `_layout()` now points at `/`; breadcrumb on `/conversations/{id}` follows. New `HOME_CSS` constant (~330 lines) is isolated from `BASE_CSS` so the homepage's typography and full-bleed sections don't fight the constrained `<main>` shell the rest of the app uses. New per-feature doc `docs/web-ui.md` covers routes, the homepage design system (tokens / typography / motion / responsive), conversations index, transcript view, Markdown rendering, export format, SSE tick loop, ingest, auth, and a "where to look for what" table. Smoke-tested via `TestClient` (homepage, conversations index, populated state, breadcrumb path) + browser-verified at 1440×900 in Chrome (initial 7.6vw hero clamp was overflowing the asymmetric grid; tightened to 5.6vw / 80px ceiling with -0.04em tracking). Closes part of the "Adopt per-feature documentation pattern" Open row. |
| 2026-05-06 | Web UI: "Export Conversation" button + Markdown download | Conversation detail page picks up an Export Conversation button next to the live indicator; clicking downloads a `.md` file. Filename derived from the topic via `_topic_slug(topic, max_len=25)` + `_export_filename(cid, topic)`: ASCII-only, lowercased, runs of non-alphanumeric collapsed to single hyphens, truncated to 25 chars. Falls back to `conversation-<id>.md` when no usable ASCII remains. Both `Content-Disposition: attachment; filename=...` and the page's `<a download="...">` agree on the slugged name. Server-side: `_render_export_markdown(data)` builds the document (`# Conversation #{id}: {topic}` heading, metadata table, one `## {sender} — {timestamp}` section per message with the body verbatim, footer `_Exported from Agent Battleground._`). New `GET /api/conversations/{cid}/export.md` route returns it with `text/markdown` content-type; 404 on unknown id. `.btn` CSS gained `display: inline-block` + `text-decoration: none` so the anchor renders like the existing buttons. Smoke-tested in-process via Starlette's `TestClient` plus 9 unit cases for the slug helper (em-dashes, mixed CJK/ASCII, all-non-ASCII fallback, empty, single char, punctuation-only). Follow-up captured in Open table: word-boundary slug truncation if mid-word cuts on real topics become annoying. |
| 2026-05-06 | Web UI: rebrand header / page title to "Agent Battleground" | `_layout()` now renders `<title>{page} — Agent Battleground</title>` and `<h1><a href="/">Agent Battleground</a></h1>`. Visible everywhere the shell renders. Protocol-level identifiers (auth realms, env var names, module docstring, argparse description, startup log) deliberately left as `agent_chat` to avoid breaking saved browser credentials, the sidecar bearer-token client, and any external script that greps stdout. |
| 2026-05-06 | Validate SSE live-append against an active conversation | Validated end-to-end during the operator-flow shakedown. Multiple active conversations watched on `https://agent-chat.mikesailab.com/conversations/<id>` with new rows streaming in via the `event: message` SSE channel within ~5s of each local `send_message`. Hosted side received deltas via `scripts/db_sync.py` → `POST /api/ingest`; server-side SSE picked them up automatically without code changes (the design intent). `event: complete` on natural conversation close still partially unproven and is covered by the Open "Exercise unexercised completion code paths" row. |
| 2026-05-06 | `scripts/start.ps1` — sidecar lifecycle + seed-conversation wrapper | New PowerShell wrapper that ensures the `db_sync.py` sidecar is up and forwards remaining args to `start_conversation.py`. Detects existing launchers by filtering `Get-CimInstance Win32_Process` on `ExecutablePath -ieq <venv python>` so the launcher's base-interpreter child (the second `python.exe` every Windows venv invocation produces) doesn't double-count as a duplicate. Behaviours: 0 launchers → spawn fresh sidecar in a `pwsh -NoExit` window via `Start-Process`; 1 launcher → reuse it; >1 → warn with PID list and exit 1. `-Force` cascade-kills via `Stop-SidecarTree`: kills the launcher's python child first, then the launcher, then the spawned `pwsh -NoExit` window itself (only if its CommandLine matches `*-NoExit*` AND `*db_sync.py*`, so the user's interactive shell can never be killed by mistake). `-SidecarOnly` skips the seed step. Trailing args forwarded verbatim to `src/start_conversation.py`, exit code propagated. New `docs/db-sync.md` Setup tip points first-time readers at the wrapper. New troubleshooting subsections: "Two `python.exe` processes per sidecar (this is normal)" explains the venv-launcher pattern with the correct logical-count query, and "Multiple sidecar launchers running (the real duplicate case)" covers the genuine race scenario plus a warning that Windows leaves stale `ParentProcessId` values when a parent dies and its PID gets recycled. |
| 2026-05-05 | Local → Fly DB sync (push-based mirror) | Live-mirror local `db/chat.db` writes to the Fly deploy so the hosted Web UI reflects local agent conversations within ~5s. Two additions, no changes to `agent_chat_mcp.py`: (a) **`POST /api/ingest`** in `src/web_ui.py` — bearer-token auth via new `AGENT_CHAT_INGEST_TOKEN` env var (constant-time compared, opt-in per deploy: returns `404 ingest disabled` when unset), single-transaction body `{conversations, messages, deleted_conversation_ids}`, `INSERT OR REPLACE` for conversations, `INSERT OR IGNORE` for messages, manual cascade on conversation deletes (FK off in this codebase), idempotent. `BasicAuthMiddleware` short-circuits on `/api/ingest` so the bearer route is its own auth realm — machine clients don't need the basic-auth password. (b) **`scripts/db_sync.py`** — stdlib-only sidecar (`urllib.request`, `sqlite3`, `argparse`, `signal`, `logging`) that watermarks `db/.sync-state.json` (`last_message_id`, `conversations_updated_after`, `known_conversation_ids`), reads deltas each tick, computes deletions via set-difference, POSTs the batch with `Authorization: Bearer <AGENT_CHAT_INGEST_TOKEN>`, advances watermarks only on 200. Daemon (default 5s interval) and `--once` modes. Fatal on 401/403/404 (config error, exit 2); transient on network/5xx (retry next tick). Direction strictly local → Fly: force-stop on the hosted UI does **not** propagate back; local sidecar will re-upsert the active row on the next tick. Smoke-tested in-process with Starlette's `TestClient` — 19 assertions across nine paths (ingest disabled, missing/wrong/right bearer, valid POST landing in DB, idempotent re-post, deletion cascading to messages, basic-auth bypass on `/api/ingest`, browser paths still gated, malformed JSON → 400, clean `import db_sync`). New per-feature doc `docs/db-sync.md` covers architecture, setup (token gen, `fly secrets set`, local env), daemon vs `--once`, flag table, full endpoint reference, troubleshooting, security notes, and a "why not Litestream" rationale (Windows-first project; Litestream official builds are Linux/macOS only). `.gitignore` updated for `db/.sync-state.json`. |
| 2026-05-04 | Web UI: render message content as Markdown | Messages now render through `markdown-it-py` (`gfm-like` preset, `html: False`, `breaks: True`) so bold, italics, lists, blockquotes, fenced code blocks, GFM tables, strikethrough, inline code, and bare URLs all display properly. Links carry `target="_blank" rel="noopener noreferrer"` (custom render rule). Same renderer used for the initial page load and SSE-streamed updates — server pre-renders `content_html` into the SSE payload, JS injects via `innerHTML` (no client-side Markdown lib needed). XSS posture: raw HTML in source is escaped (`html: False`), `javascript:` URLs are rejected by markdown-it-py's URL-scheme validator, and the only attack surface is the rendered output we control. New CSS for typography on `<p> <ul> <ol> <li> <strong> <em> <code> <pre> <blockquote> <a> <h1>-<h6> <table> <hr> <del>` inside `.msg-body` matches the dark theme. New deps pinned: `markdown-it-py==4.0.0`, `linkify-it-py==2.1.0`, `mdurl==0.1.2`, `uc-micro-py==2.0.0`. Smoke-tested against 18 cases (all formatting features + XSS attempts including raw `<script>`, `<img onerror>`, and `javascript:` URLs). Unblocks the syntax-highlighting follow-up. |
| 2026-05-04 | Per-CLI installation/onboarding doc — Gemini | Created `docs/clis/gemini.md`: where Gemini reads MCP config (`.gemini/settings.json`, per-folder), the exact `agent_chat` JSON snippet to paste, verification step, and a 3-agent run recipe. First instance of the per-feature documentation pattern. Replaced the off-topic full-stack-dev `agents/gemini_agent1/GEMINI.md` with a tester role doc mirroring the Claude Code / Codex versions. Added `.gemini/` to `.gitignore` (matches the `.claude/` and `.codex/` patterns) so local CLI state — including any API keys other servers need — stays out of the public repo. Backfill of equivalent docs for claude-code and codex split out as a follow-up Roadmap item. |
| 2026-05-04 | Web UI: force-stop button on conversation detail page | New `POST /api/conversations/{cid}/stop` endpoint in `src/web_ui.py` that mirrors the SQL `inspect_conversations.cmd_stop` runs (`status='complete'`, `end_reason='stopped by operator'`, `current_turn=NULL`). Idempotent — already-complete returns 200 with `already_complete: true`; missing returns 404; non-POST returns 405. Conversation detail page picks up a red `Stop conversation` button next to the live indicator (only shown while `status='active'`); click flow is `confirm()` → POST → existing SSE `event: complete` flips the indicator and removes the button automatically. Smoke-tested in-process via Starlette's `TestClient` across six paths (404 missing, active→complete + DB state, idempotent second stop, GET→405, button rendered on active, button hidden on complete). |
| 2026-05-04 | Reusable kickoff-prompt library | Created `prompts/Kickoff/kickoff.md` as the canonical reusable kickoff prompt. Uses `wait_for_turn` for the loop, includes `{{TOPIC}}` / `{{TONE}}` placeholders, and ships a small library of `{{TONE}}` examples (debate, code review, brainstorm, plan). Updated both tester role docs (`agents/claude-code_agent1/claude.md`, `agents/codex_agent1/AGENTS.md`) so the agents' default loop instructions match the prompt. Added `prompts/` to the root CLAUDE.md repo-layout tree. |
| 2026-05-04 | `wait_for_turn` push-style MCP tool | New `@mcp.tool() wait_for_turn` in `src/agent_chat_mcp.py`: server-side long-poll (1s interval, configurable timeout 5-300s, default 60s) that blocks until the agent's turn arrives, the conversation completes, or timeout fires. Returns the same shapes as `get_my_turn` plus a `timeout` status carrying the current `wait` payload so the agent can decide whether to call again. Internally extracted `_compute_turn_state()` helper shared by both tools to keep their state semantics in lockstep. Smoke-tested against a temp DB across all five paths (no_conversation, timeout, mid-wait turn flip → your_turn, already-complete → immediate return, get_my_turn shape unchanged). Closes the largest token-cost gap in the loop. |
| 2026-05-01 | End-to-end smoke test | Conversation #1 ran cleanly: `claude-code` opened with a turn-rotation validation prompt, `codex` replied with confirmation of `status=your_turn`, `turns_remaining=5`, and `current_turn` flipped back. Manually stopped via `inspect_conversations.py stop 1` before either agent hit max-turns or sent `signal='done'`. Core happy path proven (turn rotation, persistence across processes, both CLIs loading the MCP server). |
| 2026-05-01 | Confirm Codex CLI config loader behavior | Resolved empirically: Codex's default loader only reads `~/.codex/config.toml` and ignores per-folder `.codex/config.toml`. Acted on this by registering `[mcp_servers.agent_chat]` in `C:\Users\mikes\.codex\config.toml` and deleting the redundant in-repo config (commit `29ee1bc`). |
| 2026-05-01 | Local Web UI for live chat viewing — v1 | Single-file Starlette app at `src/web_ui.py` using existing transitive deps. Routes: `/`, `/conversations/<id>` (with SSE auto-update), `/api/conversations[/<id>]`, `/api/conversations/<id>/stream`. Binds to `127.0.0.1:8765`. User-confirmed working in browser. |
| 2026-05-02 | First real-content conversation logged | Conversation #3 — "The future of tech jobs in the world of AI" — ran 8 turns per agent, ended naturally on `signal='done'` from `codex`. Transcript and screenshot archived under `docs/agent-conversations/future-of-tech-jobs/`. First end-to-end run of the full kickoff-prompt → conversation → archive workflow. |
